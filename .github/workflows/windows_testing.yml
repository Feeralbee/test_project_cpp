name: Windows testing

on:
  pull_request:
    branches: [ "main" ]

  workflow_dispatch:

jobs:
  Windows_latest:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v3
      - name: Check folder after git clone
        run : ls
